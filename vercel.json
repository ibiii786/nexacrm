{
  "buildCommand": "npm install --include=dev && npm run build --workspace=@nexacrm/shared && cd apps/api && npx prisma generate --schema=prisma/schema.prisma && npx prisma migrate deploy && npx prisma db seed && cd ../../apps/web && npm run build",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "apps/api/prisma/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}